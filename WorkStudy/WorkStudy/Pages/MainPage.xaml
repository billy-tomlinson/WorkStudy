<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:workStudy="clr-namespace:WorkStudy.ViewModels" xmlns:local="clr-namespace:WorkStudy.Custom" xmlns:views="clr-namespace:WorkStudy.Pages" x:Class="WorkStudy.MainPage" BackgroundColor="Transparent" x:Name="MainPage1">
    <ContentPage.Content>
        <AbsoluteLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand" 
                        BackgroundColor="Transparent">
            <views:PageNotAllowed IsVisible="{Binding IsPageUnavailableVisible}"/>
            <StackLayout BackgroundColor="Transparent" IsVisible="{Binding IsPageVisible}">
                <views:StudyDetails />
                <StackLayout Orientation="Horizontal" Padding="2,2,2,2" HorizontalOptions="Center">
                    <Label Text="Observation Round : " FontSize="15" TextColor="Black" HorizontalOptions="Start" />
                    <Label Text="{Binding ObservationRound}" FontSize="15" TextColor="Black" />
                    <Label Text="  Time : " FontSize="15" TextColor="Black" />
                    <TimePicker Time="{Binding CurrentTime}" FontSize="15" TextColor="Black" IsEnabled="false" />
                </StackLayout>
                <local:CustomListView x:Name="List" Margin="0,10" 
                                      ItemsSource="{Binding OperatorObservations}" 
                                      BackgroundColor="Gray" HasUnevenRows="True" 
                                      ItemClickedCommand="{Binding ItemClickedCommand}" 
                                      HorizontalOptions="Center" HeightRequest="450">
                    <x:Arguments>
                        <ListViewCachingStrategy>RecycleElement</ListViewCachingStrategy>
                    </x:Arguments>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <StackLayout Padding="20">
                                    <StackLayout Orientation="Horizontal">
                                        <Label Text="{Binding Name}" FontSize="25" TextColor="Azure" />
                                        <Label Text="{Binding ActivityName}" FontSize="15" TextColor="Azure" />
                                        <Label Text="{Binding Rating}" FontSize="15" TextColor="Azure" />
                                    </StackLayout>
                                </StackLayout>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </local:CustomListView>
                <StackLayout>
                    <Button Text="Save Observations" WidthRequest="200" FontSize="10" BackgroundColor="#E8AD00" TextColor="Black" Command="{Binding SaveObservations}" />
                </StackLayout>
                <StackLayout>
                    <Button Text="Pause Study" WidthRequest="200" FontSize="10" BackgroundColor="#E8AD00" TextColor="Black" Command="{Binding PauseStudy}" />
                </StackLayout>
                <StackLayout>
                    <Button Text="Finish Study" WidthRequest="200" FontSize="10" BackgroundColor="Red" TextColor="White" Command="{Binding EndStudy}" />
                </StackLayout>
                <StackLayout>
                    <Button Text="Edit Activities" WidthRequest="200" FontSize="10" BackgroundColor="Red" TextColor="White" Command="{Binding EditStudy}" />
                </StackLayout>
            </StackLayout>
            <views:ActivitiesView IsVisible="{Binding ActivitiesVisible}" HeightRequest="150"/>
            <views:RatingView IsVisible="{Binding RatingsVisible}" Padding="10, 150, 20 ,0" />
            <StackLayout HorizontalOptions="Center" VerticalOptions="Center" AbsoluteLayout.LayoutBounds="0.25, 0.25, 1, 1" AbsoluteLayout.LayoutFlags="All">
                <views:ValidationView x:Name="validationView" IsVisible="{Binding IsInvalid}" />
            </StackLayout>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>