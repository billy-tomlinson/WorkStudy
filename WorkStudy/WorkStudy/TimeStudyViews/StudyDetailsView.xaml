<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="TimeStudy.Views.StudyDetailsView">
    <ContentView.Content>
        <StackLayout Orientation="Horizontal" HorizontalOptions="Center">
            <Label Text="Study Number : " IsVisible="{Binding Isvisible}" FontSize="20" />
            <Label Text="{Binding StudyNumber}" IsVisible="{Binding Isvisible}" FontSize="20" />
        </StackLayout>
    </ContentView.Content>
</ContentView>